'use strict';
// Write an O(n) algorithm to sort an array of integers, where you know in advance what the lowest and highest values are.
// [0,1,5,3,2,7]->0,1,2,3,5,7

function swap(array, i, j) {
  const tmp = array[i];
  array[i] = array[j];
  array[j] = tmp;
  //   return array;
}


function fromScratch(arr){


}
